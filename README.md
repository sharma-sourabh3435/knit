# knit
This is a simplified version of a version control system  
like git (it relies on same command names). Primary  
goal is to learn the inner workings of git commands and  
how the project changes are handled by VCS.